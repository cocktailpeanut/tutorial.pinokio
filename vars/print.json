{
  "run": [{
    "method": "log",
    "params": {
      "json2": {
        "pip": "{{pip}}",
        "arch": "{{arch}}",
        "platform": "{{platform}}",
        "gpu": "{{gpu ? gpu : 'none'}}"
      }
    }
  }, {
    "method": "log",
    "params": {
      "json2": {
        "time": "{{time}}",
        "system": "{{system}}",
        "cpu": "{{cpu}}",
        "mem": "{{mem}}",
        "battery": "{{battery}}",
        "shell": "{{shell}}",
        "osInfo": "{{osInfo}}",
        "audio": "{{audio}}",
        "bluetooth": "{{bluetooth}}",
        "graphics": "{{graphics}}"
      }
    }
  }]
}
