{
  "run": [{
    "method": "log",
    "params": {
      "json2": {
        "pip": "{{pip}}",
        "arch": "{{arch}}",
        "platform": "{{platform}}",
        "gpu": "{{gpu ? gup : 'none'}}"
      }
    }
  }]
}
