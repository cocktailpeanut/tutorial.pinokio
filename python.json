{
  "run": [{
    "method": "shell.run",
    "params": {
      "message": [
        "{{platform === 'win32' ? 'where' : 'which'}} python",
        "{{platform === 'win32' ? 'where' : 'which'}} python3"
      ]
    }
  }]
}
