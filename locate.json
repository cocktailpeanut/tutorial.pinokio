{
  "run": [{
    "method": "input",
    "params": {
      "title": "Setup",
      "form": [{
        "key": "command",
        "title": "command"
      }]
    }
  }, {
  "run": [{
    "method": "shell.run",
    "params": {
      "message": "{{platform === 'win32' ? 'where' : 'which'}} {{input.command}}"
    }
  }]
}
